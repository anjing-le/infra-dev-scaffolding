---
description: RESTful API 设计规范
globs: **/controller/**/*.java
alwaysApply: false
---

# RESTful API 设计规范

## URL 设计

### 基本规则
- 使用小写字母和连字符（kebab-case）
- 使用名词复数形式表示资源
- 层级关系用路径表示

### 示例
```
GET    /api/users           # 获取用户列表
POST   /api/users           # 创建用户
GET    /api/users/{id}      # 获取单个用户
PUT    /api/users/{id}      # 更新用户
DELETE /api/users/{id}      # 删除用户
```

### API 路径常量
所有路径在 `ApiConstants` 中统一管理，避免硬编码。

Controller 的 `@RequestMapping`、`@GetMapping`、`@PostMapping` 等路径必须引用 `ApiConstants`。如果新增资源路径，也要同步补充 `ApiConstants`，方便后续前端 `ApiPaths` 或 OpenAPI 生成对齐。
新增运行模块前先确认 `contracts/service-boundaries.json` 中是否已有边界；没有则先补 boundary、basePath、owner、currentHost 和 route，再写 Controller。

推荐结构：

```java
@RequestMapping(ApiConstants.Notice.BASE)
public class NoticeController {
    @GetMapping(ApiConstants.Notice.LIST)
    public APIResponse<PageResult<NoticeVO>> list(...) {
        ...
    }
}
```

模块路径约定见 `project_document/API_PATH_GUIDE.md`：每个模块提供 `BASE`，子路径使用相对路径，需要给远程调用或文档使用时提供 `*_FULL`。

## 请求规范

### HTTP 方法
| 方法 | 用途 | 是否幂等 |
|------|------|---------|
| GET | 查询 | 是 |
| POST | 创建 | 否 |
| PUT | 全量更新 | 是 |
| PATCH | 部分更新 | 否 |
| DELETE | 删除 | 是 |

### 参数传递
- **GET**: `@RequestParam` 或 `@PathVariable`
- **POST/PUT**: `@RequestBody`
- **路径资源 ID**: `@PathVariable`
- **认证信息**: `@RequestHeader("Authorization")`
- **请求上下文**: 不要在 Controller 手动生成 requestId。统一由 `RequestContextFilter` 读取或生成 `X-Request-Id` / `X-Trace-Id`，并写入 `GlobalRequestContextHolder`。
- **请求头常量**: 需要读取 `X-Request-Id`、`X-Trace-Id`、`X-Tenant-Id`、`X-Time-Zone`、`Accept-Language` 等头时，使用 `RequestHeaderConstants`。
- **OpenAPI 契约**: 真实业务接口使用明确 Request / Response DTO，不要用 `Map<String, Object>` 承载业务 payload。需要让 `/v3/api-docs` 更清楚时，在 DTO/VO 上补充 `@Schema`。

## 响应规范

### 统一格式
```json
{
  "code": "0",
  "message": "操作成功",
  "data": {},
  "timestamp": 1700000000000,
  "requestId": "0b4b3b0c-7b7b-4ed1-8c53-2ac0e73d2f6a"
}
```

### 成功响应
```java
APIResponse.success(data);           // 有数据
APIResponse.success(data, "创建成功"); // 有数据 + 自定义消息
APIResponse.success();               // 无数据
APIResponse.successMessage("删除成功"); // 无数据 + 自定义消息
APIResponse.successData("pong");       // String 等容易和消息混淆的数据
```

### 错误响应
```java
throw new BizException(XxxErrorCode.SOME_ERROR);  // 业务异常
APIResponse.error(XxxErrorCode.SOME_ERROR);        // 直接返回错误
```

### 错误码分段
错误码必须遵循 `project_document/ERROR_CODE_GUIDE.md`。

- `1000-1499`: 系统、数据库、缓存、配置等通用基础设施错误
- `1500-1599`: 分布式锁
- `1600-1899`: 远程调用，其中 `1800-1899` 默认可重试
- `1900-1999`: 状态机和流程编排
- `2000-2099`: 通用业务错误
- `2100-2399`: 登录、权限、账号、Token
- `2400-2999`: 新业务模块，按模块每 100 个 code 分配
- `3000-3999`: 参数和校验错误
- `4000-4999`: 全局安全、网关、限流、跨服务鉴权

新增 `XxxErrorCode` 前先确认分段，不要复用已有 code。

### 分页响应
```java
APIResponse.success(PageResult.of(list, total, current, size));
APIResponse.success(PageResult.of(page.getContent(), page.getTotalElements(), page.getNumber() + 1, page.getSize()));
```

分页 payload 字段固定为 `records`、`current`、`size`、`total`。`PageResult` 是共享契约对象，不直接依赖 Spring Data Page；如使用 Spring Page，在 Service/Controller 中展开字段。`BaseResponse`、`MultiResponse`、`PageResponse` 是旧远程响应兼容类，新业务接口不要使用。

## 版本管理

- 当前使用路径中的 `/api/` 前缀
- 需要版本化时使用：`/api/v1/`, `/api/v2/`

## 时间与全球化

- 服务端默认按 UTC 处理存储和接口时间，避免依赖部署机器本地时区。
- 需要当前时间时优先使用 `DateUtils.nowInstant()` / `DateUtils.nowUtc()` / `DateUtils.nowIso()`，不要直接使用 `Instant.now()` 或 `LocalDateTime.now()`。
- 需要自定义 UTC 格式时使用 `DateUtils.now(pattern)`，需要 epoch milliseconds 时使用 `DateUtils.nowEpochMilli()`。
- 需要用户展示时区时，从 `GlobalRequestContextHolder.current().getTimeZone()` 获取客户端透传的时区，再显式转换。

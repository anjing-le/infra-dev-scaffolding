package com.anjing.config.jpa;

import org.springframework.context.annotation.Configuration;

/**
 * JPA配置类
 * 
 * @author Backend Template
 * @since 2025-01-20
 */
@Configuration
public class JpaConfig {
    
    // JPA配置在application.yml中已配置
    // 这里可以添加自定义的JPA配置
}

# 课程大纲 — 脚手架：完整演示 + 工程化结构 + AI 协作入门

> **时长**：10-15 分钟  
> **定位**：所有 Agent 项目的第一节课，让学员看到全貌、理解结构、学会用 Cursor 扩展  
> **前提**：无需编程经验，重点是"理解 + 会用"，不是从零写代码

---

## 步骤 1：完整演示（3 分钟）

> **目标**：先让学员看到"这个脚手架长什么样、能干什么"，建立直觉

### 说什么

> "我们先不看代码，先看这个脚手架做出来的东西长什么样。所有后续的 Agent 项目——知识库、AIGC、智能客服——都是在这个脚手架上长出来的。"

### 屏幕展示什么

#### 1.1 登录页（30 秒）

- **展示**：浏览器打开 `http://localhost:13006`，展示登录页面
- **操作**：点击「游客访问」按钮
- **说明**："这里有正常登录和游客模式，游客不需要后端，直接体验 UI"

#### 1.2 工作台 Dashboard（30 秒）

- **展示**：登录后的控制台页面
- **操作**：简单浏览各区块——统计卡片、销售概览图表、新用户/活跃用户、待办事项
- **说明**："这是工作台，数据是 Mock 的，实际项目里对接真实 API 就行"

#### 1.3 系统管理（30 秒）

- **展示**：依次点开「系统管理」下的用户管理、角色管理、菜单管理
- **操作**：展示表格、搜索、分页、新增/编辑弹窗
- **说明**："标准的后台管理 CRUD 页面，以后你的业务页面就长这样"

#### 1.4 主题切换 + 国际化（30 秒）

- **展示**：右上角设置面板
- **操作**：
  1. 切换暗色主题 → 切回亮色
  2. 切换语言 English → 切回中文
- **说明**："暗色主题、国际化，这些功能脚手架都内置了，不用自己写"

#### 1.5 异常页（30 秒）

- **展示**：点开异常页面（403/404/500）
- **操作**：快速浏览
- **说明**："异常页、结果页这些通用组件也都有，直接用就行"

---

## 步骤 2：项目结构速览（3 分钟）

> **目标**：让学员理解"代码在哪里"，前后端怎么分的

### 说什么

> "看完了长什么样，我们打开 IDE 看看代码是怎么组织的。你不需要记住每个文件，只需要知道'找东西去哪找'。"

### 屏幕展示什么

#### 2.1 项目根目录（30 秒）

- **展示**：在 Cursor 中打开项目根目录
- **说明**：
  ```
  agent-dev-scaffolding/
  ├── frontend/      ← Vue 3 + Vite 前端
  ├── backend/       ← Spring Boot 后端
  ├── docs/          ← 文档
  └── README.md      ← 项目说明
  ```
- **强调**："整个项目就两大块——前端和后端，物理分离，互不干扰"

#### 2.2 前端目录结构（1 分钟）

- **展示**：展开 `frontend/src/` 目录树
- **逐个点开说明**：

| 目录 | 一句话说明 | 展示方式 |
|------|----------|---------|
| `views/` | 页面放这里 | 展开看到 auth/dashboard/system |
| `router/` | 路由在这里配 | 打开 `routes/modules/` 看模块化路由 |
| `api/` | 前端调后端的接口 | 展开看到 auth.ts / user.ts |
| `components/` | 通用组件 | 提一下 core/ 下的布局组件 |
| `store/` | Pinia 状态管理 | 提一下 user/menu/setting |
| `locales/` | 国际化语言包 | 展开看 zh.json / en.json |
| `config/` | 配置中心 | 提一下主题、菜单等配置 |

- **说明**："想加新页面？去 `views/` 建文件，去 `router/` 配路由。想加新接口？去 `api/` 写。结构很清晰"

#### 2.3 后端目录结构（1 分钟）

- **展示**：展开 `backend/src/main/java/com/anjing/` 目录树
- **逐个点开说明**：

| 目录 | 一句话说明 | 展示方式 |
|------|----------|---------|
| `controller/` | 接口入口 | 打开看到 AuthController、TestController |
| `config/` | 配置类 | 提一下 redis/、properties/ |
| `model/` | 数据模型 | 展开看 request/response/errorcode |
| `aspect/` | AOP 切面 | 提一下日志、分布式锁 |
| `util/` | 工具类 | 提一下 JsonUtils、PasswordUtils |
| `example/` | 功能示例 | 提一下远程调用、状态机 |

- **说明**："后端是标准的 Controller → Service → Repository 分层，想加新接口就从 Controller 开始写"

---

## 步骤 3：Cursor AI 协作亮点（4 分钟）

> **目标**：这是本课最核心的部分——现场演示"一段话让 AI 生成一个完整页面"

### 说什么

> "这个脚手架最大的亮点，不是代码本身，而是它内置了一套完整的 Cursor AI 协作配置。我现在给大家演示一下，用一段提示词，让 AI 直接生成一个新页面出来。"

### 屏幕展示什么

#### 3.1 先快速展示 Rules（30 秒）

- **展示**：在 Cursor 侧边栏展开 `frontend/.cursor/rules/` 目录
- **操作**：不用逐个打开，只展示文件列表
- **说明**："这里有 12 条前端规范、后端也有 5 条。这些规范 AI 在写代码时会自动遵守，等下你就能看到效果。"

#### 3.2 现场演示：一段提示词生成完整页面（3.5 分钟）

- **操作流程**：

1. **打开 Cursor 对话框**（`Cmd+L` 或 `Cmd+I`）

2. **粘贴以下提示词**（提前复制好，课上直接粘贴）：

---

**【演示用提示词 — 录制前提前复制到剪贴板】**

```
帮我在脚手架前端新增一个「公告管理」页面，要求：

1. 页面文件：src/views/notice/index.vue
2. 路由配置：在 src/router/modules/ 下新增 notice.ts，路径 /notice，菜单名"公告管理"，图标用 ri:megaphone-line，不限制角色（游客也能看到）
3. 路由注册：在 src/router/modules/index.ts 中导入并加到 routeModules
4. 国际化：在 zh.json 和 en.json 的 menus 下新增 notice 的翻译

页面内容：
- 顶部搜索栏：一个关键词输入框 + 搜索按钮
- 表格展示：列包括 公告标题、发布人、发布时间、状态（标签）、操作
- 用 Mock 数据写死 3-5 条示例数据（不调后端接口）
- 操作列有"查看"和"删除"按钮（点击弹 ElMessage 提示即可）
- 底部分页组件

请参考现有的系统管理页面风格，保持一致。
```

---

3. **等待 AI 生成**（通常 30-60 秒）
4. **Apply 所有文件**，切到浏览器刷新

- **预期效果**：
  - 侧边栏出现「公告管理」菜单
  - 点进去是完整的列表页：搜索栏 + 表格 + 分页
  - 切换英文菜单名也是英文的

- **讲解要点**（边等 AI 生成边说）：
  - "注意看，我没有指定用什么 UI 组件库、没有指定代码风格，但 AI 会自动用 Element Plus、`<script setup>`、TypeScript——因为 `.cursor/rules/` 里的规范它全读了"
  - "路由格式、国际化结构，也不用我记，AI 会参考现有代码保持一致"

- **生成完毕后**：
  - "大家看，一段话，四个文件，一个完整的管理页面就出来了。以后你要加新功能，就是这个流程"

> **录制提示**：如果 AI 生成超过 60 秒，可以提前录好一个生成成功的版本剪辑进去。建议录制前先跑一遍确认效果。

---

## 步骤 4：怎么基于脚手架长出新项目（2 分钟）

> **目标**：让学员明白后续课程的工作方式

### 说什么

> "这个脚手架不是让你天天看的，而是让你复制出去，在上面长出新项目。后面的知识库、AIGC、客服项目，都是这么来的。"

### 屏幕展示什么

#### 4.1 fork 流程（1 分钟）

- **展示**：打开 README.md，滚动到「基于脚手架创建新项目」章节
- **念出关键步骤**（不需要实际操作）：
  ```
  第一步：复制项目  cp -r agent-dev-scaffolding my-new-project
  第二步：改标识    改 pom.xml / package.json / application.yml 里的项目名
  第三步：清理示例  前端运行 pnpm clean:dev，后端删 TestController 和 example/
  第四步：加业务    用 Cursor Prompts 生成新的页面和接口
  ```
- **说明**："就四步，复制 → 改名 → 清理 → 开发。以后的每个项目都是这个流程"

#### 4.2 保留什么 vs 删除什么（1 分钟）

- **展示**：用两列对比（可以口述，也可以展示提前准备好的对比）：

| 保留（骨架） | 删除（示例） |
|-------------|------------|
| Cursor Rules + Prompts | TestController |
| 全局异常处理 | example/ 目录 |
| 统一响应 APIResponse | 演示页面（Dashboard 数据） |
| 权限框架 | Mock 数据 |
| 主题 / 国际化 | — |
| 工具类 / 配置类 | — |

- **说明**："骨架保留，示例删除。前端有个 `pnpm clean:dev` 一键清理演示内容"

---

## 步骤 5：核心配置概览（2 分钟）

> **目标**：让学员知道"哪些能力可以开关"，按需启停

### 说什么

> "最后看一下配置。这个脚手架的中间件和功能都是可配置的，不需要的就关掉，不会报错。"

### 屏幕展示什么

#### 5.1 后端中间件开关（1 分钟）

- **展示**：打开 `backend/src/main/resources/application.yml`，滚动到 `app.features` 部分
- **操作**：高亮展示以下配置项：

```yaml
app:
  features:
    redis:
      enabled: true        # Redis 缓存——不装 Redis 就改 false
    distributed-lock:
      enabled: true        # 分布式锁
      provider: redisson   # 可改成 local（本地锁，无需 Redis）
    database:
      enabled: true        # 数据库
    middleware:
      kafka:
        enabled: false     # Kafka——默认关闭
      minio:
        enabled: false     # MinIO——默认关闭
```

- **说明**：
  - "每个中间件都有开关，`enabled: false` 就完全不加载，即使你没装这个软件也不报错"
  - "比如你本地没装 Redis，把 redis.enabled 改成 false 就行"
  - "分布式锁还支持降级到本地锁，单机开发不需要 Redis"

#### 5.2 前端配置速览（1 分钟）

- **展示**：打开 `frontend/.env.development`
- **说明**：
  ```
  VITE_BASE_URL = /          ← 部署路径
  VITE_API_URL = /           ← API 地址（开发用 /，走 Vite 代理）
  VITE_API_PROXY_URL = http://localhost:18080   ← 后端地址
  ```
- **操作**：提一下游客模式的配置
  - "前端 `.env` 里有个 `VITE_ACCESS_MODE = frontend`，这是游客模式的前提"
- **说明**："前端配置很简单，改个后端地址、部署路径就行"

---

## 课程结尾（30 秒）

### 说什么

> "今天我们看了脚手架的全貌：完整的后台管理 UI、清晰的前后端分层结构、内置的 Cursor AI 规范和模板、灵活的配置开关。下节课我们就在这个脚手架上开始做真正的 Agent 项目。"

### 屏幕展示什么

- 回到 README.md 首页，展示技术栈 badges，留几秒钟给学员截图

---

## 演示中适合展示的亮点功能清单

| 类别 | 亮点 | 演示难度 | 视觉效果 |
|------|------|---------|---------|
| UI | 游客模式一键体验 | ⭐ 极易 | ⭐⭐⭐ 直观 |
| UI | 暗色/亮色主题实时切换 | ⭐ 极易 | ⭐⭐⭐ 惊艳 |
| UI | 中英文国际化切换 | ⭐ 极易 | ⭐⭐⭐ 直观 |
| UI | 响应式布局（侧边栏折叠） | ⭐ 极易 | ⭐⭐ 不错 |
| UI | 完整 CRUD 页面（用户/角色/菜单管理） | ⭐ 极易 | ⭐⭐⭐ 标准后台 |
| AI | 12+5 条 Cursor Rules 自动规范代码 | ⭐⭐ 需讲解 | ⭐⭐⭐ 核心卖点 |
| AI | 6 个 Prompt 模板一键生成代码 | ⭐⭐ 需演示 | ⭐⭐⭐ 核心卖点 |
| 工程 | 前后端分离 + 清晰分层 | ⭐ 展示目录即可 | ⭐⭐ 教学价值高 |
| 工程 | 中间件开关（一行配置启停） | ⭐ 展示 yml 即可 | ⭐⭐ 实用 |
| 工程 | 统一响应 + 全局异常处理 | ⭐⭐ 需 curl 演示 | ⭐⭐ 教学价值高 |
| 工程 | TestController 内存 CRUD 演示 | ⭐⭐ 需启动后端 | ⭐⭐ 可选 |
| 工程 | 前端清理脚本 `pnpm clean:dev` | ⭐ 提一句即可 | ⭐ 实用 |

---

## 录制前检查清单

### 环境检查

- [ ] Node.js 20+ 已安装并激活（`nvm use 20 && node -v`）
- [ ] pnpm 已安装（`pnpm -v`）
- [ ] 前端依赖已安装（`cd frontend && pnpm install`）
- [ ] 前端能正常启动（`pnpm dev`，访问 `http://localhost:13006`）
- [ ] Cursor IDE 已安装并登录

### 服务启动

- [ ] 前端开发服务器运行中（`http://localhost:13006`）
- [ ] 浏览器已打开登录页（不要提前登录，留给演示）

### Cursor 准备

- [ ] 用 Cursor 打开 `agent-dev-scaffolding` 项目
- [ ] 左侧文件树展开到合适层级
- [ ] 确认 `frontend/.cursor/rules/` 文件可见
- [ ] **演示提示词已复制到剪贴板**（见步骤 3 中的提示词）
- [ ] **提前跑过一次提示词**，确认生成效果正常，然后 `git checkout .` 还原（关键！）
- [ ] 确认还原后侧边栏没有「公告管理」菜单（留给演示时出现）

### 要提前打开的文件/页面

| 序号 | 文件/页面 | 用于步骤 |
|------|----------|---------|
| 1 | 浏览器：`http://localhost:13006` 登录页 | 步骤 1 |
| 2 | Cursor：项目根目录 | 步骤 2 |
| 3 | Cursor：`frontend/.cursor/rules/` 目录（展开看到文件列表） | 步骤 3 |
| 4 | 剪贴板：演示提示词（提前复制好） | 步骤 3 |
| 5 | Cursor：`README.md`（滚动到"基于脚手架创建新项目"章节） | 步骤 4 |
| 6 | Cursor：`backend/src/main/resources/application.yml` | 步骤 5 |
| 7 | Cursor：`frontend/.env.development` | 步骤 5 |

### 录制设置

- [ ] IDE 字体调大（建议 16px+）
- [ ] 浏览器缩放到 100%（确保 UI 完整展示）
- [ ] 关掉不相关的浏览器标签页和通知
- [ ] 录屏软件就绪，分辨率 1920×1080
- [ ] 麦克风测试通过

### 注意事项

- **不启动后端**：这节课只演示前端游客模式 + 代码结构 + AI 配置，不需要后端运行
- **不实际跑 clean:dev**：步骤 4 只是讲解流程，不要真的清理掉演示内容
- **步骤 3 演示是重点**：提前跑一遍提示词确认效果，然后 `git checkout .` 还原。录制时再跑一遍。如果 AI 生成超过 60 秒，可用预录片段剪辑
- **暗色主题切换后记得切回**：避免后续步骤截图风格不一致
- **Node 版本**：当前系统默认 Node 18，需要先 `nvm use 20` 才能启动前端
